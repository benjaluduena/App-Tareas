<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo negro claro -->
    <solid android:color="#1E1E1E" />

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />

    <!-- Padding opcional (si se aplica directamente como fondo) -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>